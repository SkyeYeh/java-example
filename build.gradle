allprojects {
    group 'com.skye-yeh'
    version '1.0-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'

    // java
    sourceCompatibility = 1.8

    dependencies {
        testCompile 'junit:junit:4.11'
    }
}
